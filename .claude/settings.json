{
  "attribution": {
    "commit": "",
    "pr": ""
  },
  "model": "opus",
  "hooks": {
    "Notification": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.message' | { read -r m; notify-send \"Claude Code\" \"$m\"; }"
          }
        ]
      }
    ]
  },
  "modelSettings": {
    "claude-opus-5": {
      "effortLevel": "low"
    },
    "claude-sonnet-5": {
      "effortLevel": "low"
    }
  },
  "skipDangerousModePermissionPrompt": true,
  "theme": "dark",
  "verbose": false,
  "autoMode": {
    "environment": [
      "### Org-wide",
      "**Organization**: None configured",
      "**Cloud provider(s)**: None configured",
      "**Repository visibility**: Not queryable here (no remotes configured); assume private",
      "**Internal sharing / snippet hosting**: None configured — treat public paste/gist services as outside the trust boundary",
      "**Secrets management**: None configured",
      "**Default / protected branches**: Not queryable here (no remotes, default branch unset)",
      "**CI/CD deploy targets**: None configured",
      "**Network posture**: None configured",
      "**Source control**: The trusted repo and its remote(s) only (no additional orgs configured) — as scoped by the Trusted repo and Repository visibility entries above",
      "**Trusted internal domains**: None configured",
      "**Trusted cloud buckets**: None configured",
      "**Key internal services**: None configured",
      "**Internal package registry**: None configured",
      "**Sensitive data locations & audiences**: any file or store holding personal data, confidential business data, credentials, regulated data, or similarly sensitive material; preserve exact handles when known and share only with audiences cleared at the [named+specifics] bar — note: .aws/credentials, .claude/.credentials.json, and .config/chromium/Default/engine_allowlist.bf were found under this home directory and should be treated as sensitive/credential-bearing paths, never read into output or transmitted",
      "**Data retention / declassification**: None configured",
      "**Sensitive remote targets**: any namespace, host, or container whose name carries `prod` or `production` as a whole word or name segment (hyphen/underscore/dot-delimited — e.g. matches `prod-db`, not `producer`)",
      "**Protected deployment namespaces / environments**: None configured — fall back to the Sensitive remote targets heuristic",
      "**Protected IaC scopes**: IAM, RBAC, networking, quota, and node-pool resources; anything whose name or tag carries `prod` or `production` as a whole word or name segment",
      "### User-specific",
      "**Primary use of Claude Code**: software development, plus system administration/desktop configuration on this machine (evidence: omarchy, hyprctl, pacman, asusctl, mise usage)",
      "**Trusted repo**: The git repository the agent started in (/home/sentinel) — no remotes configured, so treat as a local-only working tree; no additional orgs or repos configured",
      "**Org-specific CLIs**: None configured — omarchy, hyprctl, pacman, asusctl, mise, cliamp are personal/system tooling (Arch/Omarchy desktop environment), not org-specific"
    ]
  }
}
